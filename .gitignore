# never commit tunnel configs (keys, company endpoints)
*.conf

# local working notes, never published
CLAUDE.md
.claude/

# build artifacts (anchored so cmd/wiretray/ stays tracked)
/wiretray
/wiretray.exe
/dist/
